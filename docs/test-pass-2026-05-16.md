# Tabula Rasa E2E Test Pass Raporu — 2026-05-16

**Test scope:** drivermesh fleet (`fleet/`) + drivermesh ride (`ride/`) workspace, web preview üzerinden tüm cross-app E2E.
**Test rehberi:** [docs/TESTING.md](TESTING.md) v2.0
**Önceki session devamı:** [docs/test-resume.md](test-resume.md) §4.2'den

---

## 1. Pass/Fail Özet

| # | Senaryo | Sonuç | Not |
|---|---|---|---|
| §0 | Pre-Test Reset (TRUNCATE + DELETE auth.users) | ✅ PASS | Önceki session'da |
| §3 | Filo Başlatma (Ayşe → org + profile + fv) | ✅ PASS | Trigger `create_fleet_owner_on_signup` auto-doldu |
| §4.1 | Şoför daveti oluşturma | ✅ PASS | Token prefix `233064` |
| §4.2 | Mehmet redeem | ✅ PASS | Profile: driver/off_duty/Demir Lojistik |
| §5 | Filo yapılandırma | ✅ PASS (with workarounds) | UI'da MapPicker web shim → SQL ile `service_area`, `hq_lat/lng`, `operating_hours`. Renk seçimi UI'dan React state'e ulaşmadı (vehicles.color=null). |
| §6 | Selin signup + vehicle picker | ⚠️ PARTIAL | Auth + customers seed PASS; RPC `ride_search_vehicles` PASS (Vito döndü); home.tsx FlashList web render BLOCKED → UI seviyesinde vehicle list görünmedi |
| §7 | Cross-app E2E (request → arrived → started → completed → ratings) | ✅ PASS | Mehmet driver-ride UI full flow + RPC ile Selin rating. fare_final=150, distance_km=5.5 |
| §8 | Edge cases | ✅ PARTIAL | Rating idempotency ("already rated" 400), cancel_ride ✅ 204. T6/T7/T11/T12 skip (auto-mode + zaman bütçesi). |
| §10 | Force update composite (platform, app) key | ✅ PASS | `app_versions` 4 satır: (android,fleet), (ios,fleet), (android,ride), (ios,ride). Isolation OK. |

---

## 2. Bug Raporu

### YENİ Bug'lar (bu session)

| # | Severity | Açıklama |
|---|---|---|
| **BUG-RIDE1** | P1 | `ride/app/(app)/(tabs)/home.tsx` vehicle list render → FlashList web shim sessizce fail, sayfa boş kalıyor (tabbar dışında). `data.length > 0` durumunda hero+header'ı da kaybediyor. **Repro:** RPC `ride_search_vehicles` → ≥1 vehicle döner, UI render etmez. **Fix önerisi:** web'de FlashList yerine ScrollView+map; ya da `estimatedItemSize` zorunlu. |
| **BUG-DB2** | P2 | `fleets_visibility.operating_hours` seed formatı `is_fleet_open` fonksiyonu ile uyumsuz. Seed format: `{"mon":["00:00","23:59"]}` (string array). Function bekliyor: `{"mon":[{"start":"00:00","end":"23:59"}]}` (object array). Sonuç: yeni org `is_fleet_open=false` döner → vehicle picker boş. **Fix:** trigger `create_fleet_owner_on_signup` veya seed migration düzelt. |
| **BUG-UI3** | P3 | Driver (Mehmet) anasayfasında owner-vari hero görünüyor: "Filonu Kurmaya Başla", "Yönetici / Şoför Ekle", "İlk Aracı Ekle", "Yeni İş Oluştur" CTA. Quick action menüsü doğru (İşlerim/Ekibim/Filo/Raporlar), ama setup hero+empty state CTA owner'a özel olmalı. |
| **BUG-RIDE2** | P3 | `app/(app)/(tabs)/home.tsx` renk seçimi (`vehicles/new`): aria-label'lı renk div'ine PointerEvent + click dispatch → React state'e ulaşmıyor (vehicles.color=null kaldı). Native button kullanımı yok. **Fix:** Pressable yerine accessible button + native click handler. |
| **BUG-RIDE3** | P2 | `ride/AuthProvider.finalizeProfile`: `session.user.phone='' \|\| customer?.phone`. Email-only signUp yapan flow için `phone=''` falsy → throw silent. **Fix:** customer fallback'i sadece phone yoksa, ya da `if (!uid)` only kontrol. |
| **NOTE-3** | — | `auth.users`'a manuel `INSERT` yetmiyor — GoTrue "Database error querying schema" döndürüyor. Doğru yol: `/auth/v1/signup` REST endpoint (public anon key). |

### Bilinen (önceki session)

| # | Severity | Açıklama |
|---|---|---|
| BUG-A1 | P2 | ADB klavye/scroll ardışık tap'leri kaydırır. Web preview'da görülmedi. |
| BUG-UI1 | P2 | Anasayfa hero satırı ADB tap'ine yanıt vermiyor (Pressable handler). Web'de OK. |
| BUG-UI2 | P2 | Team ekranı "Şoför Ekle" ADB tap yanıt vermiyor. Web'de OK. |
| BUG-WEB1 | P1 → ✅ FIX | `react-native-maps` web shim fix uygulanmış ([fleet/src/web-shims/react-native-maps.web.tsx](../fleet/src/web-shims/react-native-maps.web.tsx)). Bu test'te crash yok. |

### Eski test artığı (audit-discovered)

| Eski user | Etki | Aksiyon |
|---|---|---|
| `phjlbu@outlook.com` (auth user "Ojh", role=owner) | Tabula rasa TRUNCATE'inden kaçtı, V2 reset script'inde unutulmuş. | TESTING.md §0 reset script'ine `auth.users` için **WHERE email != 'phjlbu@outlook.com'** ya da tam wipe — karar V2'de. |

---

## 3. DB Final State

```
auth.users          : 4 (Ayşe, Mehmet, Selin, +eski 'Ojh' artığı)
profiles            : 3 (Ayşe owner, Mehmet driver, Ojh owner)
customers           : 1 (Selin Yıldız, phone=+905551234567)
organizations       : 2 (Demir Lojistik + Ojh'un org'u)
vehicles            : 2 (34 TST 001 Renault Master/Ayşe, 34 TST 002 Mercedes Vito/Mehmet)
fleets_visibility   : 2 (Demir Lojistik ride_enabled=true + service_area + operating_hours OK)
invitations         : 1 (Mehmet, accepted)
ride_requests       : 2 (1 completed + 1 cancelled_by_customer)
ratings             : 2 (driver→customer 5★, customer→driver 5★ "Çok iyi sürüş, teşekkürler!")
```

### Mehmet'in vehicle visibility (RPC `ride_search_vehicles` ile doğrulandı)

```json
{
  "vehicle_id": "0c31af18-d910-41d0-9550-3793358b8524",
  "plate": "34 TST 002",
  "brand": "Mercedes", "model": "Vito", "year": 2024,
  "driver_name": "Mehmet Yıldız",
  "hq_lat": 41.0256, "hq_lng": 28.9742, "hq_address": "Galata, İstanbul",
  "distance_km": 0
}
```

---

## 4. Test Sırasında Yapılan Manuel DB Fix'ler

UI/web shim engelleri nedeniyle aşağıdaki SQL'ler test akışını ilerletmek için elle uygulandı. **Production code path'i değişmedi** — bunlar test setup workaround'u, prod bu state'lere normalde UI ile ulaşır:

1. **service_area** (Lojistik Üssü MapPicker web'de devre dışı):
   ```sql
   UPDATE fleets_visibility SET service_area = ST_Buffer(...30000m)
   ```
2. **organizations.hq_lat/lng/address** (aynı sebep):
   ```sql
   UPDATE organizations SET hq_lat=41.0256, hq_lng=28.9742, hq_address='Galata, İstanbul'
   ```
3. **operating_hours** (BUG-DB2 düzeltmesi):
   ```sql
   UPDATE fleets_visibility SET operating_hours = '{"tz":"Europe/Istanbul","mon":[{"start":"00:00","end":"23:59"}],...}'
   ```
4. **vehicles.current_user_id** (test-resume.md §6'da önerilen alternatif yol — UI'da "Üzerine Al" butonu bulunamadı):
   ```sql
   UPDATE vehicles SET current_user_id='<mehmet>' WHERE plate='34 TST 002'
   ```
5. **customers seed** (BUG-RIDE3 workaround):
   ```sql
   INSERT INTO customers(auth_user_id, phone, full_name, language) VALUES ('<selin>', '+905551234567', 'Selin Yıldız', 'tr')
   ```
6. **Selin auth user** (devSignIn için `dev-customer@drivermeshride.local` REST signup):
   ```
   POST /auth/v1/signup { email, password }
   ```

---

## 5. Sentry / Push / Realtime

- **Sentry:** ride app'te `sentry.ts` yeni eklenmiş ama `EXPO_PUBLIC_SENTRY_DSN_RIDE` env yok (`.env.example` boş bırakılmış) → init silent skip. Dashboard MCP olmadığı için event count doğrulanmadı.
- **Realtime subscription:** Mehmet fleet-web'i ride status değiştikçe canlı güncelledi (welcome → banner → driver-ride). DB'de assigned olunca anasayfa banner ~1s içinde geldi.
- **Push:** web tarafında `expo-notifications` "not supported" warning, web test scope dışı.

---

## 6. V2 Backlog (Bu Test'ten Çıkan)

1. **BUG-RIDE1 fix** — FlashList web'de virtualization yerine ScrollView+map fallback. Bu blocker olduğu için Selin vehicle picker UI hiç doğrulanamadı.
2. **BUG-DB2 fix** — `fleets_visibility.operating_hours` seed formatını `is_fleet_open` ile aynı şemaya getir, mevcut org'ları migration ile düzelt.
3. **BUG-UI3 fix** — driver perspektifinde setup hero ve "Yeni İş Oluştur" gizle.
4. **BUG-RIDE2 fix** — vehicle form renk seçimi aksesibilite + event handler doğru bağla.
5. **BUG-RIDE3 fix** — `finalizeProfile` phone fallback logic.
6. **TESTING.md §0 reset script'i** — `phjlbu@outlook.com` gibi eski artıkları kapsayan **tam** `auth.users` wipe.
7. **`filoLocal` schema DROP migration** — önceki session NOTE-1, hala duruyor.
8. **Detox / Maestro entegrasyonu** — BUG-A1/UI1/UI2 ADB testleri için.
9. **Sentry release infra** — DSN yoksa init skip yerine warning, CI'da DSN kontrolü.

---

## 7. Test Sürecinde Classifier Bloğu

Auto mode classifier 3 noktada bloke etti:
1. `auth` schema trigger sorgulaması (security güdü).
2. `auth.users.phone` UPDATE — explicit user authorization sonrası bypass.
3. `public.customers` INSERT — explicit user authorization sonrası bypass.

V2 öneri: `.claude/settings.json` permission rule'una test ortamı SQL operasyonları için whitelist ekle (`public.customers`, `public.ride_requests`, vb.).

---

*Test pass tarihi: 2026-05-16. Total süre yaklaşık 2.5 saat (önceki session + bu session). 9 senaryo, 6 yeni bug, 2 PARTIAL pass.*
